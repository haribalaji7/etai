import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from '@/components/providers';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'NexusAI — AI Autonomous Enterprise Workflow Platform',
  description: 'The AI brain your enterprise needs. 15 autonomous AI agents, intelligent workflows, and enterprise-grade analytics — all in one platform.',
  keywords: ['AI', 'enterprise', 'workflow', 'automation', 'agents'],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
